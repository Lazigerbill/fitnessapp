<%= form_for [@instructor, @appointment] do |f| %>
	<%= f.label :session_start, "Choose session start time" %>
	<%= f.datetime_select :session_start, :ampm => true, :minute_step => 30 %>
	<%= f.submit "Proceed to payment" %>
<% end %>